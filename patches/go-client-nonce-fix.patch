diff --git a/pkg/ark-lib/tree/musig2.go b/pkg/ark-lib/tree/musig2.go
index ea5014e..9036fec 100644
--- a/pkg/ark-lib/tree/musig2.go
+++ b/pkg/ark-lib/tree/musig2.go
@@ -361,7 +361,10 @@ func (t *treeSignerSession) AggregateNonces(txid string, pubkeyNonces map[string
 
 	myNonce, ok := t.myNonces[txid]
 	if !ok {
-		return false, fmt.Errorf("missing my nonce for txid %s", txid)
+		// We are not a cosigner for this txid — skip gracefully.
+		// This happens when the server does not filter events by topic and
+		// forwards TreeNonces for tree nodes where this signer has no key.
+		return false, nil
 	}
 
 	if t.aggregateNonces == nil {
@@ -369,7 +372,10 @@ func (t *treeSignerSession) AggregateNonces(txid string, pubkeyNonces map[string
 	}
 
 	if aggNonce, ok := t.aggregateNonces[txid]; ok && aggNonce != nil {
-		return len(t.aggregateNonces) == len(t.txs), nil // skip: we already have the aggregated nonce for this txid
+		// Compare against myNonces (only txids we're a cosigner for) instead of
+		// txs (all tree nodes) so that signers who only participate in a subset
+		// of the tree can correctly detect completion.
+		return len(t.aggregateNonces) == len(t.myNonces), nil
 	}
 
 	// set our nonce to ensure server didn't overwrite it
@@ -401,7 +407,9 @@ func (t *treeSignerSession) AggregateNonces(txid string, pubkeyNonces map[string
 	}
 
 	t.aggregateNonces[txid] = &Musig2Nonce{aggregatedNonce}
-	return len(t.aggregateNonces) == len(t.txs), nil
+	// Compare against myNonces count — we only need aggregated nonces for txids
+	// where we are a cosigner.
+	return len(t.aggregateNonces) == len(t.myNonces), nil
 }
 
 // Sign generates the musig2 partial signatures for each transaction of the tree that includes the
diff --git a/pkg/client-lib/batch_session_handler.go b/pkg/client-lib/batch_session_handler.go
index 2e8fea8..c941746 100644
--- a/pkg/client-lib/batch_session_handler.go
+++ b/pkg/client-lib/batch_session_handler.go
@@ -451,9 +451,11 @@ func (h *defaultBatchEventsHandler) OnTreeSignatureEvent(
 func (h *defaultBatchEventsHandler) OnTreeSigningStarted(
 	ctx context.Context, event client.TreeSigningStartedEvent, vtxoTree *tree.TxTree,
 ) (bool, error) {
+	fmt.Printf("[DEBUG OnTreeSigningStarted] cosigners_pubkeys=%v\n", event.CosignersPubkeys)
 	foundPubkeys := make([]string, 0, len(h.signerSessions))
 	for _, session := range h.signerSessions {
 		myPubkey := session.GetPublicKey()
+		fmt.Printf("[DEBUG OnTreeSigningStarted] my_pubkey=%s in_cosigners=%v\n", myPubkey[:16], slices.Contains(event.CosignersPubkeys, myPubkey))
 		if slices.Contains(event.CosignersPubkeys, myPubkey) {
 			foundPubkeys = append(foundPubkeys, myPubkey)
 		}
@@ -532,6 +534,11 @@ func (h *defaultBatchEventsHandler) OnTreeSigningStarted(
 func (h *defaultBatchEventsHandler) OnTreeNonces(
 	ctx context.Context, event client.TreeNoncesEvent,
 ) (bool, error) {
+	nonceKeys := make([]string, 0, len(event.Nonces))
+	for k := range event.Nonces {
+		nonceKeys = append(nonceKeys, k[:8]+"...")
+	}
+	fmt.Printf("[DEBUG OnTreeNonces] txid=%s nonce_pubkeys=%v topic=%v\n", event.Txid[:8], nonceKeys, event.Topic)
 	log.Debugf("tree nonces event received for tx %s", event.Txid)
 	if len(h.signerSessions) <= 0 {
 		return false, fmt.Errorf("tree signer session not set")
@@ -540,25 +547,32 @@ func (h *defaultBatchEventsHandler) OnTreeNonces(
 	handler := func(session tree.SignerSession) (bool, error) {
 		hasAllNonces, err := session.AggregateNonces(event.Txid, event.Nonces)
 		if err != nil {
+			fmt.Printf("[DEBUG OnTreeNonces] AggregateNonces ERROR txid=%s err=%v\n", event.Txid[:8], err)
 			return false, err
 		}
 
+		fmt.Printf("[DEBUG OnTreeNonces] AggregateNonces txid=%s hasAllNonces=%v\n", event.Txid[:8], hasAllNonces)
+
 		if !hasAllNonces {
 			return false, nil
 		}
 
+		fmt.Printf("[DEBUG OnTreeNonces] all nonces aggregated, signing...\n")
 		log.Debugf("all nonces aggregated, signing...")
 		sigs, err := session.Sign()
 		if err != nil {
+			fmt.Printf("[DEBUG OnTreeNonces] Sign ERROR: %v\n", err)
 			return false, err
 		}
 
+		fmt.Printf("[DEBUG OnTreeNonces] submitting tree signatures for batch=%s pubkey=%s\n", event.Id[:8], session.GetPublicKey()[:16])
 		if err := h.client.SubmitTreeSignatures(
 			ctx,
 			event.Id,
 			session.GetPublicKey(),
 			sigs,
 		); err != nil {
+			fmt.Printf("[DEBUG OnTreeNonces] SubmitTreeSignatures ERROR: %v\n", err)
 			return false, err
 		}
 
